config = [
    {
        "resolution": 4,
        "transition_epochs": 0,
        "stabilization_epochs": 10,
        "batch_size": 128,
        "channels": 512,
    },
    {
        "resolution": 8,
        "transition_epochs": 10,
        "stabilization_epochs": 10,
        "batch_size": 128,
        "channels": 256,
    },
    {
        "resolution": 16,
        "transition_epochs": 10,
        "stabilization_epochs": 10,
        "batch_size": 128,
        "channels": 128,
    },
    {
        "resolution": 32,
        "transition_epochs": 10,
        "stabilization_epochs": 10,
        "batch_size": 128,
        "channels": 64,
    },
    {
        "resolution": 64,
        "transition_epochs": 10,
        "stabilization_epochs": 10,
        "batch_size": 64,
        "channels": 32,
    },
    {
        "resolution": 128,
        "transition_epochs": 10,
        "stabilization_epochs": 10,
        "batch_size": 64,
        "channels": 16,
    },
]
